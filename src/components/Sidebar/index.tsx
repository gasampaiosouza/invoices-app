import { Container, Logo, LogoContainer } from './styles';

const Sidebar = () => {
  return (
    <Container>
      <LogoContainer>
        <Logo />
      </LogoContainer>
    </Container>
  );
};

export default Sidebar;
